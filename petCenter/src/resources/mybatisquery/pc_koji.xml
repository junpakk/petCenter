<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
          "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
          
<mapper namespace="main.pc.koji.dao.KojiDAO">

<insert id="petkojiInsert" parameterType="kojivo" >
 
	INSERT INTO PC_KOJI (
								mid
								,ktitle
								,kcontent
								,delyn
								,idate
								,udate
								)
            			VALUES( 
            					#{mid, jdbcType=VARCHAR }, 
            					#{ktitle, jdbcType=VARCHAR }, 
            					#{kcontent, jdbcType=VARCHAR }, 
            					 
            					         					
            					'Y', 
            					SYSDATE, 
            					SYSDATE)
</insert>
<select id="kojiSelectAll" parameterType="kojivo" resultType="kojivo">
	SELECT
	A.MID AS MID
	,A.KTITLE AS KTITLE
	,A.KCONTENT AS KCONTENT
	FROM PC_KOJI A
	WHERE A.DELYN='Y'

</select>
<update id="kojiDelete" parameterType="kojivo">
UPDATE PC_KOJI
SET 
	DELYN = 'N'
	WHERE DELYN = 'Y'
	AND MID = #{mid}
</update>
<select id="kojiSelect" parameterType="kojivo" resultType="kojivo">
	SELECT
	A.MID AS MID
	,A.KTITLE AS KTITLE
	,A.KCONTENT AS KCONTENT
	FROM PC_KOJI A
	WHERE A.DELYN='Y'
	AND A.MID = #{mid}

</select>
<update id="kojiUpdate" parameterType="kojivo">
UPDATE PC_KOJI
SET
	KCONTENT = #{kcontent}
	WHERE DELYN ='Y'
	AND MID = #{mid}
</update>
</mapper>